language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t adamokasha/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true adamokasha/docker-react npm run test
